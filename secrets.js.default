// The credentials in this file are used to get an X-Auth-Token from the
// identity provider. The X-Auth-Token will be used for subsequent API calls.

module.exports = {
    username: 'foo',
    password: 'bar'
};